# Measured Impact

This project is a governed investment data product built for portfolio holdings, exposure, and reconciliation reporting. Metrics on this page describe **measured technical controls and reporting readiness**, not production business outcomes.

## What This Document Is

A factual record of what the build currently demonstrates. Every value here is backed by an artifact in the repo — a dbt test, a model output, a reconciliation table, or a log line. If a metric cannot be verified from the repo, it does not appear on this page.

## What This Document Is Not

This is not a claim of production deployment, buy-side adoption, assets under management coverage, business cost savings, analyst time saved, or investment decision impact. The product demonstrates how I structure governed investment data. It does not claim market deployment.

## Current Measured Controls

*Last verified: 2026-05-06 18:31 dbt test — 127 of 127 passing*

### Model Coverage

| Layer | Models | Purpose |
|---|---:|---|
| Bronze (staging views) | 6 | Raw source standardization |
| Silver dimensions | 6 | Conformed reference data (security, issuer, manager, date) incl. SCD history + current |
| Silver facts | 3 | Position, transaction, price observations |
| Risk marts | 7 | Exposure, concentration, benchmark coverage, valuation |
| Reconciliation gates | 4 | Position-to-NAV, benchmark coverage, stale prices, rating coverage |
| dbt snapshots | 1 | SCD capture (dim_security_snapshot, strategy: check) |
| **Total** | **27** | All compiling, all building |

### Test Coverage

| Test Category | Count | Severity |
|---|---:|---|
| `not_null` | 43 | Blocking |
| `unique` | 22 | Blocking |
| `accepted_values` | 15 | Blocking |
| `dbt_utils.expression_is_true` | 42 | Blocking |
| Custom singular tests | 2 | Blocking |
| **Total** | **127** | **All blocking, no warn-only overrides** |

**Most recent test run:** 127 of 127 passing (2026-05-06 18:31)

### Type 2 SCD Coverage

| Dimension | SCD Implemented | Pattern |
|---|---|---|
| dim_security | Yes (v0.1) | dbt snapshot, check strategy on 6 attribute columns |
| dim_issuer | Planned (v0.2) | Same pattern as dim_security |
| dim_manager | Planned (v0.2 with SEC 13F) | Same pattern |
| dim_date | N/A | Conformed dimension, no history needed |

Surfaced via `dim_security_history` (table, full version history) and `dim_security_current` (view, point-in-time current state).

### Reconciliation Framework

Four reconciliation models writing to the `recon` schema:

1. **Position-to-NAV reconciliation** — validates fact-level holdings totals against fund NAV
2. **Benchmark coverage** — measures completeness of benchmark prices for in-portfolio securities
3. **Stale price detection** — flags securities with prices older than tolerance
4. **Rating coverage** — measures completeness of credit rating data for fixed-income holdings

Output is queryable as relational tables, not file artifacts. A reconciliation summary CSV is planned for v0.2.

### Data Provenance

| Source | Coverage | Type |
|---|---|---|
| yfinance equity prices | Real | Public market data via API |
| Bond prices | Synthetic | Deterministic RNG, seeded |
| Credit ratings | Synthetic | Deterministic RNG, seeded |
| Positions | Synthetic | Deterministic RNG, seeded |

All synthetic data is generated by `scripts/ingest_seed_data.py` with fixed seeds for reproducibility. Public regulatory data ingestion (SEC 13F) is planned for v0.3.

### Reporting Layer

Static HTML dashboard (`dashboard/index.html`, 1,191 lines) renders all 7 risk marts and 4 reconciliation gates using Chart.js 4.4.2. Data is embedded as JavaScript at build time.

## What These Metrics Demonstrate

- **Data quality discipline before reporting publication.** 127 blocking tests means no model publishes without passing referential and value-level checks. Coverage includes Type 2 SCD invariants (grain uniqueness, exactly-one-current per natural key, no overlapping version ranges) enforced via custom singular tests.
- **Reconciliation as a first-class concern.** Four reconciliation gates are part of the standard build, not an afterthought.
- **Reporting readiness.** Marts are shaped for downstream consumption, with a working dashboard rendering every output.
- **Reproducibility.** Synthetic data is deterministic. Real data is sourced via documented APIs. Anyone can rebuild this product end-to-end.

## What These Metrics Do Not Demonstrate

- Production deployment to a buy-side or insurance investment team
- Real-world assets under management coverage
- Stakeholder adoption metrics
- Business outcomes (cost savings, time savings, decision quality)
- SEC 13F or N-PORT public regulatory data ingestion (planned, not yet built)
- Foreign-key referential integrity tests (planned for v0.2)

## Roadmap to Stronger Claims

The metrics on this page improve as the build advances:

| Version | Adds | New Honest Claim |
|---|---|---|
| v0.2 | Type 2 SCD, FK tests, bronze tests, CI | Historical reporting, referential integrity, automated regression gates |
| v0.3 | SEC 13F ingestion, recon summary file | Real public regulatory data, source-to-curated reconciliation with measured variance |
| v0.4 | N-PORT, risk metrics, Power BI build | Fund-level coverage, duration/credit metrics, alternative consumer layer |

## How to Verify Any Claim on This Page

Every metric here can be reproduced locally:

```bash
# Model and test counts
dbt parse
dbt ls --resource-type model | wc -l
dbt ls --resource-type test | wc -l

# Run all tests
dbt build

# Inspect reconciliation output
psql -d analytics_demo -c "SELECT * FROM recon.position_nav_reconciliation;"
```

If anything in this document does not match the repo, the repo is the source of truth. File an issue or update this page.
